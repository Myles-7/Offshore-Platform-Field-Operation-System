
-- Auto-generated: knowledge_case + maintenance_process for H2 test schema.

CREATE TABLE IF NOT EXISTS knowledge_case (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  case_no VARCHAR(64) NOT NULL,
  title VARCHAR(255) NOT NULL,
  equipment_type VARCHAR(128),
  fault_type VARCHAR(128),
  fault_phenomenon CLOB,
  fault_cause CLOB,
  solution CLOB,
  preventive_measures CLOB,
  work_type VARCHAR(64),
  enabled_flag TINYINT NOT NULL DEFAULT 1,
  source_work_order_id BIGINT,
  attachment_ids VARCHAR(500),
  local_id VARCHAR(128),
  server_id BIGINT,
  version INT NOT NULL DEFAULT 1,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag TINYINT NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS maintenance_process (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  process_code VARCHAR(64) NOT NULL,
  process_name VARCHAR(255) NOT NULL,
  equipment_type VARCHAR(128),
  process_type VARCHAR(128),
  process_steps CLOB,
  tools_required CLOB,
  material_required CLOB,
  safety_measures CLOB,
  quality_standard CLOB,
  duration_estimate VARCHAR(64),
  work_type VARCHAR(64),
  enabled_flag TINYINT NOT NULL DEFAULT 1,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INT NOT NULL DEFAULT 1,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag TINYINT NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);
