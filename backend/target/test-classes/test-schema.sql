-- Auto-generated H2 test schema aligned with V1__init_schema.sql.

CREATE TABLE IF NOT EXISTS sys_user (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  username VARCHAR(64) NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  real_name VARCHAR(64) NOT NULL,
  phone VARCHAR(32),
  email VARCHAR(128),
  employee_no VARCHAR(64),
  avatar_file_id VARCHAR(64),
  account_status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  pc_enabled INTEGER NOT NULL DEFAULT 1,
  mobile_enabled INTEGER NOT NULL DEFAULT 1,
  primary_project_id BIGINT,
  department_id BIGINT,
  last_login_time TIMESTAMP,
  last_login_ip VARCHAR(64),
  password_updated_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sys_role (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  role_code VARCHAR(64) NOT NULL,
  role_name VARCHAR(64) NOT NULL,
  role_type VARCHAR(32) NOT NULL DEFAULT 'BUSINESS',
  data_scope VARCHAR(32) NOT NULL DEFAULT 'SELF',
  pc_enabled INTEGER NOT NULL DEFAULT 1,
  mobile_enabled INTEGER NOT NULL DEFAULT 0,
  sort_order INTEGER NOT NULL DEFAULT 0,
  status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sys_permission (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  parent_id BIGINT,
  permission_code VARCHAR(128) NOT NULL,
  permission_name VARCHAR(128) NOT NULL,
  permission_type VARCHAR(32) NOT NULL,
  platform VARCHAR(32) NOT NULL DEFAULT 'PC',
  route_path VARCHAR(255),
  api_method VARCHAR(16),
  api_path VARCHAR(255),
  component_path VARCHAR(255),
  icon VARCHAR(64),
  sort_order INTEGER NOT NULL DEFAULT 0,
  visible_flag INTEGER NOT NULL DEFAULT 1,
  status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sys_user_role (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL,
  role_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sys_role_permission (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  role_id BIGINT NOT NULL,
  permission_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS operation_log (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  trace_id VARCHAR(64),
  operator_id BIGINT,
  operator_name VARCHAR(64),
  role_code VARCHAR(64),
  platform VARCHAR(32) NOT NULL DEFAULT 'PC',
  module_name VARCHAR(64) NOT NULL,
  operation_type VARCHAR(64) NOT NULL,
  business_type VARCHAR(64),
  business_id VARCHAR(64),
  business_no VARCHAR(128),
  project_id BIGINT,
  device_id VARCHAR(128),
  request_method VARCHAR(16),
  request_path VARCHAR(255),
  request_ip VARCHAR(64),
  user_agent VARCHAR(500),
  request_body CLOB,
  result_status VARCHAR(32) NOT NULL DEFAULT 'SUCCESS',
  error_code VARCHAR(64),
  error_message VARCHAR(500),
  operation_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS project_info (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_code VARCHAR(64) NOT NULL,
  project_name VARCHAR(128) NOT NULL,
  platform_name VARCHAR(128),
  owner_unit VARCHAR(128),
  contractor_unit VARCHAR(128),
  project_manager_id BIGINT,
  project_location VARCHAR(255),
  start_date DATE,
  end_date DATE,
  project_status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  sort_order INTEGER NOT NULL DEFAULT 0,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_template (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  template_code VARCHAR(64) NOT NULL,
  template_name VARCHAR(128) NOT NULL,
  work_type VARCHAR(64),
  default_priority VARCHAR(32) NOT NULL DEFAULT 'NORMAL',
  default_work_content CLOB,
  default_material_desc CLOB,
  default_duration_hours DECIMAL(10, 2),
  enabled_flag INTEGER NOT NULL DEFAULT 1,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_no VARCHAR(64) NOT NULL,
  project_id BIGINT NOT NULL,
  template_id BIGINT,
  work_title VARCHAR(128) NOT NULL,
  work_type VARCHAR(64),
  work_location VARCHAR(255) NOT NULL,
  work_content CLOB NOT NULL,
  required_material_desc CLOB,
  leader_id BIGINT,
  maintainer_id BIGINT,
  planned_start_time TIMESTAMP,
  planned_end_time TIMESTAMP,
  actual_start_time TIMESTAMP,
  actual_end_time TIMESTAMP,
  status VARCHAR(32) NOT NULL DEFAULT 'PENDING_ASSIGN',
  priority VARCHAR(32) NOT NULL DEFAULT 'NORMAL',
  reject_reason VARCHAR(500),
  close_reason VARCHAR(500),
  acceptance_required INTEGER NOT NULL DEFAULT 1,
  source_type VARCHAR(32) NOT NULL DEFAULT 'PC',
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_status_log (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  from_status VARCHAR(32),
  to_status VARCHAR(32) NOT NULL,
  operation_type VARCHAR(64) NOT NULL,
  operation_desc VARCHAR(500),
  operator_id BIGINT,
  operation_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_assignment (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  assigner_id BIGINT,
  assignee_id BIGINT NOT NULL,
  assignment_role VARCHAR(32) NOT NULL DEFAULT 'MAINTAINER',
  assignment_status VARCHAR(32) NOT NULL DEFAULT 'ASSIGNED',
  assigned_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  accepted_at TIMESTAMP,
  rejected_at TIMESTAMP,
  completed_at TIMESTAMP,
  reject_reason VARCHAR(500),
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_material (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  material_code VARCHAR(64),
  material_name VARCHAR(128) NOT NULL,
  material_spec VARCHAR(128),
  unit VARCHAR(32),
  planned_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  actual_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  material_desc VARCHAR(500),
  required_flag INTEGER NOT NULL DEFAULT 1,
  prepare_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  project_id BIGINT NOT NULL,
  record_no VARCHAR(64),
  record_type VARCHAR(32) NOT NULL DEFAULT 'CONSTRUCTION',
  construction_time TIMESTAMP NOT NULL,
  construction_user_id BIGINT NOT NULL,
  construction_user_name VARCHAR(64),
  construction_desc CLOB NOT NULL,
  site_condition CLOB,
  abnormal_flag INTEGER NOT NULL DEFAULT 0,
  abnormal_desc VARCHAR(1000),
  weather VARCHAR(64),
  temperature DECIMAL(6, 2),
  humidity DECIMAL(6, 2),
  location_name VARCHAR(255),
  latitude DECIMAL(10, 7),
  longitude DECIMAL(10, 7),
  altitude DECIMAL(10, 2),
  attachment_count INTEGER NOT NULL DEFAULT 0,
  ai_result_count INTEGER NOT NULL DEFAULT 0,
  record_status VARCHAR(32) NOT NULL DEFAULT 'DRAFT',
  submitted_at TIMESTAMP,
  confirmed_by BIGINT,
  confirmed_at TIMESTAMP,
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_record_detail (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  record_id BIGINT NOT NULL,
  detail_type VARCHAR(32) NOT NULL DEFAULT 'TEXT',
  detail_title VARCHAR(128),
  detail_content CLOB,
  step_no INTEGER,
  item_code VARCHAR(64),
  item_name VARCHAR(128),
  item_value VARCHAR(500),
  item_unit VARCHAR(32),
  normal_flag INTEGER,
  abnormal_desc VARCHAR(1000),
  attachment_ref_flag INTEGER NOT NULL DEFAULT 0,
  ai_ref_flag INTEGER NOT NULL DEFAULT 0,
  sort_order INTEGER NOT NULL DEFAULT 0,
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_check_item (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  record_id BIGINT,
  item_code VARCHAR(64),
  item_name VARCHAR(128) NOT NULL,
  item_type VARCHAR(32) NOT NULL DEFAULT 'BOOLEAN',
  item_desc VARCHAR(500),
  required_flag INTEGER NOT NULL DEFAULT 1,
  check_result VARCHAR(32),
  check_value VARCHAR(500),
  check_unit VARCHAR(32),
  abnormal_flag INTEGER NOT NULL DEFAULT 0,
  abnormal_desc VARCHAR(1000),
  checked_by BIGINT,
  checked_at TIMESTAMP,
  attachment_required_flag INTEGER NOT NULL DEFAULT 0,
  attachment_count INTEGER NOT NULL DEFAULT 0,
  ai_required_flag INTEGER NOT NULL DEFAULT 0,
  ai_result_count INTEGER NOT NULL DEFAULT 0,
  sort_order INTEGER NOT NULL DEFAULT 0,
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS file_storage (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  file_id VARCHAR(64) NOT NULL,
  original_name VARCHAR(255) NOT NULL,
  stored_name VARCHAR(255) NOT NULL,
  file_type VARCHAR(32) NOT NULL,
  mime_type VARCHAR(128) NOT NULL,
  file_size BIGINT NOT NULL DEFAULT 0,
  storage_type VARCHAR(32) NOT NULL DEFAULT 'LOCAL',
  bucket_name VARCHAR(128),
  file_path VARCHAR(1000) NOT NULL,
  preview_path VARCHAR(1000),
  thumbnail_path VARCHAR(1000),
  file_hash VARCHAR(128),
  checksum VARCHAR(128),
  upload_user_id BIGINT,
  upload_user_name VARCHAR(64),
  upload_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  upload_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  retry_count INTEGER NOT NULL DEFAULT 0,
  last_retry_time TIMESTAMP,
  error_code VARCHAR(64),
  error_message VARCHAR(500),
  work_order_id BIGINT,
  record_id BIGINT,
  access_level VARCHAR(32) NOT NULL DEFAULT 'PRIVATE',
  preview_enabled INTEGER NOT NULL DEFAULT 1,
  download_enabled INTEGER NOT NULL DEFAULT 1,
  cache_enabled INTEGER NOT NULL DEFAULT 1,
  cache_key VARCHAR(255),
  cache_expire_time TIMESTAMP,
  local_file_path VARCHAR(1000),
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_attachment (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  record_id BIGINT,
  file_id VARCHAR(64) NOT NULL,
  attachment_type VARCHAR(32) NOT NULL,
  attachment_name VARCHAR(255),
  attachment_desc VARCHAR(500),
  business_scene VARCHAR(64) NOT NULL DEFAULT 'WORK_RECORD',
  capture_time TIMESTAMP,
  capture_user_id BIGINT,
  capture_user_name VARCHAR(64),
  latitude DECIMAL(10, 7),
  longitude DECIMAL(10, 7),
  location_name VARCHAR(255),
  watermark_flag INTEGER NOT NULL DEFAULT 0,
  watermark_text VARCHAR(1000),
  watermark_time TIMESTAMP,
  watermark_work_order_no VARCHAR(64),
  watermark_user_name VARCHAR(64),
  watermark_latitude DECIMAL(10, 7),
  watermark_longitude DECIMAL(10, 7),
  duration_seconds INTEGER,
  media_width INTEGER,
  media_height INTEGER,
  ai_result_id BIGINT,
  ai_bind_status VARCHAR(32) NOT NULL DEFAULT 'NONE',
  preview_status VARCHAR(32) NOT NULL DEFAULT 'AVAILABLE',
  mobile_cache_status VARCHAR(32) NOT NULL DEFAULT 'NOT_CACHED',
  upload_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  retry_count INTEGER NOT NULL DEFAULT 0,
  last_retry_time TIMESTAMP,
  error_code VARCHAR(64),
  error_message VARCHAR(500),
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS employee_info (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT,
  employee_no VARCHAR(64) NOT NULL,
  real_name VARCHAR(64) NOT NULL,
  phone VARCHAR(32),
  id_card_hash VARCHAR(128),
  department_id BIGINT,
  position_name VARCHAR(128),
  employee_status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS qualification_type (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  qualification_code VARCHAR(64) NOT NULL,
  qualification_name VARCHAR(128) NOT NULL,
  warning_days INTEGER NOT NULL DEFAULT 30,
  required_flag INTEGER NOT NULL DEFAULT 0,
  enabled_flag INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS employee_certificate (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  employee_id BIGINT NOT NULL,
  qualification_type_id BIGINT NOT NULL,
  certificate_no VARCHAR(128) NOT NULL,
  certificate_name VARCHAR(128) NOT NULL,
  issue_org VARCHAR(128),
  issue_date DATE,
  valid_from DATE,
  valid_to DATE,
  valid_status VARCHAR(32) NOT NULL DEFAULT 'VALID',
  warning_level VARCHAR(32),
  file_id VARCHAR(64),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_qualification_check (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  employee_id BIGINT NOT NULL,
  certificate_id BIGINT,
  qualification_type_id BIGINT,
  check_result VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  check_time TIMESTAMP,
  checker_id BIGINT,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'SYNCED',
  device_id VARCHAR(128),
  operator_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_acceptance (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  acceptance_no VARCHAR(64) NOT NULL,
  work_order_id BIGINT NOT NULL,
  project_id BIGINT NOT NULL,
  work_order_no VARCHAR(64) NOT NULL,
  project_name VARCHAR(128),
  construction_user_id BIGINT,
  construction_user_name VARCHAR(64),
  acceptance_user_id BIGINT NOT NULL,
  acceptance_user_name VARCHAR(64),
  acceptance_time TIMESTAMP NOT NULL,
  acceptance_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  acceptance_result VARCHAR(32),
  acceptance_opinion VARCHAR(1000),
  problem_desc VARCHAR(1000),
  rectification_required INTEGER NOT NULL DEFAULT 0,
  record_summary CLOB,
  attachment_summary CLOB,
  signature_count INTEGER NOT NULL DEFAULT 0,
  pdf_generated_flag INTEGER NOT NULL DEFAULT 0,
  locked_flag INTEGER NOT NULL DEFAULT 0,
  locked_at TIMESTAMP,
  locked_by BIGINT,
  lock_reason VARCHAR(500),
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_signature (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  signature_no VARCHAR(64) NOT NULL,
  work_order_id BIGINT NOT NULL,
  acceptance_id BIGINT,
  file_id VARCHAR(64) NOT NULL,
  signature_role VARCHAR(32) NOT NULL,
  signer_user_id BIGINT,
  signer_name VARCHAR(64) NOT NULL,
  signer_phone VARCHAR(32),
  signed_at TIMESTAMP NOT NULL,
  sign_location VARCHAR(255),
  latitude DECIMAL(10, 7),
  longitude DECIMAL(10, 7),
  signature_hash VARCHAR(128),
  signature_status VARCHAR(32) NOT NULL DEFAULT 'SIGNED',
  local_file_path VARCHAR(1000),
  upload_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  retry_count INTEGER NOT NULL DEFAULT 0,
  last_retry_time TIMESTAMP,
  error_code VARCHAR(64),
  error_message VARCHAR(500),
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_pdf (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  pdf_no VARCHAR(64) NOT NULL,
  work_order_id BIGINT NOT NULL,
  acceptance_id BIGINT NOT NULL,
  file_id VARCHAR(64) NOT NULL,
  work_order_no VARCHAR(64) NOT NULL,
  project_name VARCHAR(128),
  construction_user_name VARCHAR(256),
  acceptance_user_name VARCHAR(64),
  acceptance_time TIMESTAMP NOT NULL,
  signature_file_ids VARCHAR(1000),
  record_summary CLOB,
  pdf_content_snapshot CLOB,
  pdf_status VARCHAR(32) NOT NULL DEFAULT 'GENERATED',
  generated_by BIGINT,
  generated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  locked_flag INTEGER NOT NULL DEFAULT 1,
  archive_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  preview_enabled INTEGER NOT NULL DEFAULT 1,
  download_enabled INTEGER NOT NULL DEFAULT 1,
  local_file_path VARCHAR(1000),
  upload_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  retry_count INTEGER NOT NULL DEFAULT 0,
  last_retry_time TIMESTAMP,
  error_code VARCHAR(64),
  error_message VARCHAR(500),
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS material_info (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  material_code VARCHAR(64) NOT NULL,
  material_name VARCHAR(128) NOT NULL,
  material_category VARCHAR(64),
  material_spec VARCHAR(128),
  material_model VARCHAR(128),
  unit VARCHAR(32) NOT NULL DEFAULT '件',
  brand VARCHAR(128),
  manufacturer VARCHAR(128),
  safety_stock_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  enabled_flag INTEGER NOT NULL DEFAULT 1,
  trace_enabled INTEGER NOT NULL DEFAULT 1,
  qrcode_required INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS material_inventory (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  material_id BIGINT NOT NULL,
  material_code VARCHAR(64) NOT NULL,
  warehouse_code VARCHAR(64) NOT NULL DEFAULT 'DEFAULT',
  warehouse_name VARCHAR(128) NOT NULL DEFAULT '默认仓库',
  location_code VARCHAR(64),
  batch_no VARCHAR(64),
  qrcode_id BIGINT,
  current_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  locked_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  available_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  last_in_time TIMESTAMP,
  last_out_time TIMESTAMP,
  last_check_time TIMESTAMP,
  inventory_status VARCHAR(32) NOT NULL DEFAULT 'NORMAL',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS material_qrcode (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  material_id BIGINT NOT NULL,
  material_code VARCHAR(64) NOT NULL,
  qrcode_value VARCHAR(255) NOT NULL,
  qrcode_file_id VARCHAR(64),
  batch_no VARCHAR(64),
  serial_no VARCHAR(128),
  generate_user_id BIGINT,
  generate_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  bind_status VARCHAR(32) NOT NULL DEFAULT 'BOUND',
  qrcode_status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  last_scan_time TIMESTAMP,
  last_scan_user_id BIGINT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS material_inout_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  record_no VARCHAR(64) NOT NULL,
  material_id BIGINT NOT NULL,
  material_code VARCHAR(64) NOT NULL,
  material_name VARCHAR(128) NOT NULL,
  qrcode_id BIGINT,
  qrcode_value VARCHAR(255),
  project_id BIGINT,
  work_order_id BIGINT,
  work_order_no VARCHAR(64),
  inout_type VARCHAR(32) NOT NULL,
  quantity DECIMAL(14, 3) NOT NULL,
  before_qty DECIMAL(14, 3),
  after_qty DECIMAL(14, 3),
  warehouse_code VARCHAR(64) NOT NULL DEFAULT 'DEFAULT',
  warehouse_name VARCHAR(128) NOT NULL DEFAULT '默认仓库',
  location_code VARCHAR(64),
  batch_no VARCHAR(64),
  source_type VARCHAR(32) NOT NULL DEFAULT 'PC',
  business_reason VARCHAR(255),
  operator_id BIGINT,
  operator_name VARCHAR(64),
  operate_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  approval_status VARCHAR(32) NOT NULL DEFAULT 'APPROVED',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_material_usage (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  usage_no VARCHAR(64),
  work_order_id BIGINT NOT NULL,
  work_order_no VARCHAR(64) NOT NULL,
  project_id BIGINT NOT NULL,
  material_id BIGINT NOT NULL,
  material_code VARCHAR(64) NOT NULL,
  material_name VARCHAR(128) NOT NULL,
  material_spec VARCHAR(128),
  unit VARCHAR(32),
  qrcode_id BIGINT,
  qrcode_value VARCHAR(255),
  planned_qty DECIMAL(14, 3),
  used_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  waste_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  return_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  usage_time TIMESTAMP NOT NULL,
  usage_user_id BIGINT NOT NULL,
  usage_user_name VARCHAR(64),
  usage_location VARCHAR(255),
  usage_desc VARCHAR(500),
  cost_price DECIMAL(14, 4),
  cost_amount DECIMAL(14, 4),
  source_type VARCHAR(32) NOT NULL DEFAULT 'MOBILE',
  inout_record_id BIGINT,
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS device_info (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  device_id VARCHAR(128) NOT NULL,
  user_id BIGINT NOT NULL,
  device_name VARCHAR(128),
  platform VARCHAR(32) NOT NULL DEFAULT 'ANDROID',
  os_version VARCHAR(64),
  app_version VARCHAR(64),
  manufacturer VARCHAR(128),
  model VARCHAR(128),
  imei_hash VARCHAR(128),
  push_token VARCHAR(255),
  register_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  last_login_time TIMESTAMP,
  last_heartbeat_time TIMESTAMP,
  last_sync_time TIMESTAMP,
  last_sync_cursor VARCHAR(255),
  online_status VARCHAR(32) NOT NULL DEFAULT 'OFFLINE',
  device_status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  sync_enabled INTEGER NOT NULL DEFAULT 1,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sync_task (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  sync_task_no VARCHAR(64) NOT NULL,
  batch_id VARCHAR(128) NOT NULL,
  device_id VARCHAR(128) NOT NULL,
  operator_id BIGINT NOT NULL,
  sync_direction VARCHAR(32) NOT NULL,
  sync_type VARCHAR(32) NOT NULL DEFAULT 'INCREMENTAL',
  task_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  total_count INTEGER NOT NULL DEFAULT 0,
  success_count INTEGER NOT NULL DEFAULT 0,
  failed_count INTEGER NOT NULL DEFAULT 0,
  conflict_count INTEGER NOT NULL DEFAULT 0,
  retry_count INTEGER NOT NULL DEFAULT 0,
  max_retry_count INTEGER NOT NULL DEFAULT 5,
  request_cursor VARCHAR(255),
  response_cursor VARCHAR(255),
  client_time TIMESTAMP,
  server_start_time TIMESTAMP,
  server_end_time TIMESTAMP,
  error_code VARCHAR(64),
  error_message VARCHAR(1000),
  idempotency_key VARCHAR(128),
  request_summary CLOB,
  response_summary CLOB,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sync_log (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  sync_task_id BIGINT NOT NULL,
  batch_id VARCHAR(128) NOT NULL,
  device_id VARCHAR(128) NOT NULL,
  operator_id BIGINT NOT NULL,
  module_type VARCHAR(64) NOT NULL,
  entity_type VARCHAR(128) NOT NULL,
  action_type VARCHAR(32) NOT NULL,
  local_id VARCHAR(128),
  server_id BIGINT,
  entity_id BIGINT,
  work_order_id BIGINT,
  business_no VARCHAR(128),
  client_version INTEGER,
  server_version INTEGER,
  client_updated_at TIMESTAMP,
  server_updated_at TIMESTAMP,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  conflict_id BIGINT,
  retry_count INTEGER NOT NULL DEFAULT 0,
  checksum VARCHAR(128),
  error_code VARCHAR(64),
  error_message VARCHAR(1000),
  request_payload CLOB,
  response_payload CLOB,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS sync_conflict (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  conflict_no VARCHAR(64) NOT NULL,
  sync_task_id BIGINT,
  sync_log_id BIGINT,
  device_id VARCHAR(128) NOT NULL,
  operator_id BIGINT NOT NULL,
  module_type VARCHAR(64) NOT NULL,
  entity_type VARCHAR(128) NOT NULL,
  entity_id BIGINT,
  local_id VARCHAR(128),
  server_id BIGINT,
  work_order_id BIGINT,
  business_no VARCHAR(128),
  base_version INTEGER,
  client_version INTEGER,
  server_version INTEGER,
  client_updated_at TIMESTAMP,
  server_updated_at TIMESTAMP,
  conflict_type VARCHAR(64) NOT NULL,
  conflict_fields CLOB,
  old_payload CLOB,
  client_payload CLOB,
  server_payload CLOB,
  final_payload CLOB,
  default_strategy VARCHAR(32) NOT NULL DEFAULT 'LAST_WRITE_WINS',
  resolve_strategy VARCHAR(32),
  resolve_status VARCHAR(32) NOT NULL DEFAULT 'PENDING_REVIEW',
  resolver_id BIGINT,
  resolve_time TIMESTAMP,
  resolve_comment VARCHAR(1000),
  last_write_side VARCHAR(32),
  last_write_time TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS work_order_version_log (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  work_order_id BIGINT NOT NULL,
  work_order_no VARCHAR(64) NOT NULL,
  version INTEGER NOT NULL,
  previous_version INTEGER,
  change_source VARCHAR(32) NOT NULL,
  change_type VARCHAR(64) NOT NULL,
  changed_fields CLOB,
  old_payload CLOB,
  new_payload CLOB,
  local_id VARCHAR(128),
  server_id BIGINT,
  device_id VARCHAR(128),
  operator_id BIGINT,
  sync_task_id BIGINT,
  sync_log_id BIGINT,
  conflict_id BIGINT,
  client_updated_at TIMESTAMP,
  server_updated_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS ai_model_info (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  model_code VARCHAR(64) NOT NULL,
  model_name VARCHAR(128) NOT NULL,
  model_version VARCHAR(64) NOT NULL,
  model_type VARCHAR(32) NOT NULL DEFAULT 'DEFECT_DETECTION',
  runtime_type VARCHAR(32) NOT NULL DEFAULT 'TFLITE',
  deploy_side VARCHAR(32) NOT NULL DEFAULT 'MOBILE',
  model_file_id VARCHAR(64),
  model_hash VARCHAR(128),
  input_size VARCHAR(64),
  defect_types CLOB,
  confidence_threshold DECIMAL(6, 4) NOT NULL DEFAULT 0.5000,
  active_flag INTEGER NOT NULL DEFAULT 0,
  model_status VARCHAR(32) NOT NULL DEFAULT 'DRAFT',
  released_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS ai_result (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  ai_result_no VARCHAR(64) NOT NULL,
  work_order_id BIGINT NOT NULL,
  work_order_no VARCHAR(64) NOT NULL,
  project_id BIGINT NOT NULL,
  record_id BIGINT NOT NULL,
  attachment_id BIGINT NOT NULL,
  file_id VARCHAR(64) NOT NULL,
  result_image_file_id VARCHAR(64),
  model_id BIGINT,
  model_code VARCHAR(64),
  model_version VARCHAR(64) NOT NULL,
  infer_side VARCHAR(32) NOT NULL DEFAULT 'MOBILE',
  infer_time TIMESTAMP NOT NULL,
  infer_cost_ms INTEGER,
  defect_type VARCHAR(32) NOT NULL DEFAULT 'UNKNOWN',
  confidence DECIMAL(8, 6),
  suspected_defect_flag INTEGER NOT NULL DEFAULT 0,
  defect_count INTEGER NOT NULL DEFAULT 0,
  result_summary VARCHAR(1000),
  raw_result CLOB,
  review_status VARCHAR(32) NOT NULL DEFAULT 'PENDING_REVIEW',
  reviewed_flag INTEGER NOT NULL DEFAULT 0,
  reviewer_id BIGINT,
  review_time TIMESTAMP,
  client_created_at TIMESTAMP,
  client_updated_at TIMESTAMP,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS ai_defect_box (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  ai_result_id BIGINT NOT NULL,
  box_no VARCHAR(64),
  defect_type VARCHAR(32) NOT NULL,
  confidence DECIMAL(8, 6) NOT NULL,
  x DECIMAL(12, 4) NOT NULL,
  y DECIMAL(12, 4) NOT NULL,
  width DECIMAL(12, 4) NOT NULL,
  height DECIMAL(12, 4) NOT NULL,
  image_width INTEGER,
  image_height INTEGER,
  normalized_flag INTEGER NOT NULL DEFAULT 0,
  box_label VARCHAR(128),
  box_color VARCHAR(32),
  sort_order INTEGER NOT NULL DEFAULT 0,
  local_id VARCHAR(128),
  server_id BIGINT,
  version INTEGER NOT NULL DEFAULT 0,
  sync_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  device_id VARCHAR(128),
  operator_id BIGINT,
  conflict_flag INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS ai_review_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  review_no VARCHAR(64) NOT NULL,
  ai_result_id BIGINT NOT NULL,
  work_order_id BIGINT NOT NULL,
  record_id BIGINT NOT NULL,
  attachment_id BIGINT NOT NULL,
  reviewer_id BIGINT NOT NULL,
  reviewer_name VARCHAR(64),
  review_status VARCHAR(32) NOT NULL,
  confirmed_defect_type VARCHAR(32),
  review_opinion VARCHAR(1000),
  acceptance_suggestion VARCHAR(1000),
  review_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

CREATE TABLE IF NOT EXISTS report_daily_summary (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  summary_date DATE NOT NULL,
  project_id BIGINT,
  project_name VARCHAR(128),
  work_order_total INTEGER NOT NULL DEFAULT 0,
  work_order_in_progress INTEGER NOT NULL DEFAULT 0,
  work_order_completed INTEGER NOT NULL DEFAULT 0,
  work_order_pending_acceptance INTEGER NOT NULL DEFAULT 0,
  work_order_rejected INTEGER NOT NULL DEFAULT 0,
  completion_rate DECIMAL(8, 4) NOT NULL DEFAULT 0,
  attendance_count INTEGER NOT NULL DEFAULT 0,
  record_count INTEGER NOT NULL DEFAULT 0,
  attachment_count INTEGER NOT NULL DEFAULT 0,
  material_used_amount DECIMAL(14, 4) NOT NULL DEFAULT 0,
  material_used_qty DECIMAL(14, 3) NOT NULL DEFAULT 0,
  output_value DECIMAL(14, 4) NOT NULL DEFAULT 0,
  ai_suspected_count INTEGER NOT NULL DEFAULT 0,
  conflict_pending_count INTEGER NOT NULL DEFAULT 0,
  generated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_flag INTEGER NOT NULL DEFAULT 0,
  created_by BIGINT,
  updated_by BIGINT,
  remark VARCHAR(500)
);

